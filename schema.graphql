type Chain @entity {
  "Network identifier"
  id: ID!
  paraId: Int
  genesisHash: String
  prefix: Int
  name: String
  token: String
  decimals: Int
  existentialDeposit: String
  account: String
  rpcs: [String!]!
  parathreads: [Chain!] @derivedFrom(field: "relay")
  relay: Chain
}
